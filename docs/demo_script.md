# Demo Script

1. Run `python demo.py`.
2. Show that baseline tests fail on the bundled sample repository.
3. Show the changed file: `src/securecalc/calculator.py`.
4. Show that patched tests pass.
5. Open the generated PR report in the Streamlit dashboard.

Short pitch:

> I built a software-engineering agent that does not just write code. It reproduces a bug, patches it, verifies baseline-vs-patched behavior, audits safety/privacy risks, and generates a PR-ready report.
